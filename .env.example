# Meta Conversions API (server only — never expose in the browser or commit real values)
META_ACCESS_TOKEN=

# Optional: Meta Graph API version (default: v21.0)
# META_API_VERSION=v21.0

# Optional: test events in Meta Events Manager
# META_TEST_EVENT_CODE=
